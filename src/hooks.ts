// TODO: Enable Paraglide reroute after i18n-integration spec is implemented
// import { deLocalizeUrl } from '$lib/paraglide/runtime';
// export const reroute = (request: { url: string }) => deLocalizeUrl(request.url).pathname;
